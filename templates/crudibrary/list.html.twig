<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Libraries Table</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            font-size: 18px;
            text-align: left;
        }
        th, td {
            padding: 12px;
            border: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
        }
        tr:hover {
            background-color: #f5f5f5;
        }
    </style>
</head>
<body>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Website</th>
            <th>Date of Creation</th>
            <th>Number of Authors</th>
        </tr>
    </thead>
    <tbody>
        {% for library in list %}
        <tr>
            <td>{{ library.id }}</td>
            <td>{{ library.name }}</td>
            <td>{{ library.website }}</td>
            <td>{{ library.datecreation }}</td>
            <td>{{ library.authors|length }}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

</body>
</html>
